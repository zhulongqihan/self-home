# 后端环境变量模板（复制为 .env 后填入真实值；.env 不上传 Git）

# ====== 服务监听 ======
PORT=3000
HOST=127.0.0.1
NODE_ENV=production

# ====== MongoDB ======
MONGO_URI=mongodb://127.0.0.1:27017/couple_app

# ====== 微信小程序 ======
WX_APPID=你的小程序 AppID
WX_APPSECRET=你的小程序 AppSecret

# 订阅消息模板 ID（公众平台 → 功能 → 订阅消息 → 选用模板后复制）
# 店长：新订单提醒（字段示例 thing1 商品、amount2 金额、time3 时间）
WX_TEMPLATE_OWNER_NEW_ORDER=
# 顾客：订单状态变更（字段示例 thing1 状态、character_string2 订单号、time3 时间）
WX_TEMPLATE_CUSTOMER_ORDER_STATUS=

# ====== JWT ======
JWT_SECRET=请用 openssl rand -base64 48 生成一个随机串
JWT_EXPIRES_IN=30d

# ====== 白名单（openid 无感登录）======
OWNER_OPENID=
CUSTOMER_OPENID=

# ====== 账号密码登录（首次启动种子用，之后可后台改）======
# 启动时如果数据库里没有同名用户，会自动创建
OWNER_USERNAME=owner
OWNER_PASSWORD=请改成你自己的强密码
OWNER_NICKNAME=店长

CUSTOMER_USERNAME=customer
CUSTOMER_PASSWORD=请改成她的强密码
CUSTOMER_NICKNAME=宝宝