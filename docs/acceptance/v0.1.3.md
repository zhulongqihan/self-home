# 验收清单 v0.1.3 - 任务 3：openid 白名单鉴权 + JWT

## ✅ 本次产出

### 数据库模型（Mongoose）
- `server/src/models/User.js` - 用户表（openid 唯一索引、角色、虚拟币、签到记录）
- `server/src/models/Config.js` - 全局配置单例（白名单、店铺信息、彩蛋开关等，对应 PRD §7-10）

### 鉴权服务
- `server/src/services/wxAuth.js` - 封装 code2session 调微信 / JWT 签发与验证
- `server/src/middlewares/auth.js` - requireAuth + requireRole 中间件

### 路由
- `POST /api/auth/login` - 接收 code → 调微信换 openid → 比对白名单 → 返回 JWT 和用户信息
- `GET /api/auth/me` - 需 token，返回当前用户信息

### 启动种子
- `server/src/config/seed.js` - 首次启动自动创建 config 单例 + 从 .env 注入白名单
- 已生效：MongoDB 中 `config.global` 文档已存在

### 服务器侧
- /opt/couple-app/.env 已填入 WX_APPID + WX_APPSECRET
- PM2 进程重启正常，应用版本 0.1.3

## 🟢 验收点（curl 测试）

```bash
# 1. 健康检查（version 0.1.3）
curl https://api.cyruszhang.online/api/health

# 2. 列端点
curl https://api.cyruszhang.online/

# 3. 缺 code → 400 MISSING_CODE
curl -X POST https://api.cyruszhang.online/api/auth/login \
  -H "Content-Type: application/json" -d "{}"

# 4. 假 code → 400 WX_LOGIN_FAILED（微信侧拒绝，证明 AppID/Secret 正确）
curl -X POST https://api.cyruszhang.online/api/auth/login \
  -H "Content-Type: application/json" -d '{"code":"fake"}'

# 5. 无 token 访问受保护端点 → 401 NO_TOKEN
curl https://api.cyruszhang.online/api/auth/me
```

- [x] 5 个测试用例全部返回符合预期
- [x] MongoDB 中 config.global 单例已建
- [x] WX_APPID/SECRET 已就位（未提交 Git）
- [x] JWT_SECRET 随机生成（已就位）
- [x] 白名单当前为空（待真实登录后获取 openid 再填）

## ⚠️ 待你做的小事
1. 任务 4 完成后（小程序首次跑起来），从后端日志拿到双方真实 openid，填到 `/opt/couple-app/.env` 的 `OWNER_OPENID` 和 `CUSTOMER_OPENID`
2. 这次对话里 AppSecret 已经"暴露"在聊天记录里 → 建议任务全部完成后到微信公众平台重置一次 AppSecret，再用同样方法填回服务器

## ▶️ 下一步
任务 4：小程序前端双端入口路由 + 兜底页
- `pages/launch/index` - 启动页：自动 wx.login → 调后端 /api/auth/login
- 根据 role 路由到 customer 端或 owner 端
- 陌生人 → 兜底页"店铺暂未营业 🥺"
- token 缓存到 wx.setStorageSync，下次启动自动复用

