# 验收清单 v0.3.3 - 三大倒计时

## 店长端（暗号 `yangyang`）
- [ ] 工作台 → 系统设置 → 可**点选**「在一起起始日 / 纪念日 / 顾客生日」（日期选择器，非手输）
- [ ] 格式：起始日 `YYYY-MM-DD`，后两项 `MM-DD`
- [ ] 保存成功 toast；非法格式有错误提示

## 顾客端（暗号 `zhuzhu`）
- [ ] 「我的」页在余额卡片下方展示「我们的小日子」
- [ ] 三项数值与配置一致：在一起 X 天、距纪念日、距生日
- [ ] 未配置时显示「纪念日倒计时筹备中～」

## 接口（可选 curl）
- [ ] `GET /api/config/countdowns` 返回 `items` 数组
- [ ] 健康检查 `GET /api/health` 版本为 `0.3.3`

## 揭晓前
- 仍不添加她为体验成员，solo 验收
