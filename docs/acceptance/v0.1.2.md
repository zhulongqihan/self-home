# 验收清单 v0.1.2 - 任务 2：MongoDB + Node 后端骨架

## ✅ 本次产出

### 服务器侧（已部署）
- MongoDB 7.0.34 已安装，监听 `127.0.0.1:27017`（仅本机访问，安全）
- PM2 7.0.1 已全局安装
- `/opt/couple-app/` 部署 Node 后端代码 + npm 依赖
- `/opt/couple-app/.env` 含 JWT_SECRET（已随机生成）+ MongoDB URI（权限 600）
- PM2 进程 `couple-app` 在 `127.0.0.1:3000` 监听
- PM2 已 save + 配置开机自启
- `/var/log/couple-app/` 日志目录

### 本地代码（已提交 Git）
```
server/
├── package.json              # express/mongoose/helmet/cors/morgan/dotenv/jwt/axios
├── ecosystem.config.js       # PM2 配置（fork 模式，自动重启，300M 内存上限）
├── .env.example              # 环境变量模板（不含真实值）
├── README.md
└── src/
    ├── app.js                # Express 应用入口
    ├── config/
    │   ├── index.js          # 集中读取环境变量
    │   └── db.js             # MongoDB 连接管理
    ├── routes/
    │   └── health.js         # GET /api/health
    └── middlewares/
        └── errorHandler.js   # notFound + 全局错误处理
```

## 🟢 验收点

请用浏览器或 curl 测试：

```bash
# 1. 健康检查
curl https://api.cyruszhang.online/api/health
# 期望：{"status":"ok","version":"0.1.2","db":"connected","node":"v20.20.0",...}

# 2. 根路径
curl https://api.cyruszhang.online/
# 期望：{"name":"couple-app-server","version":"0.1.2",...}

# 3. 404 测试
curl https://api.cyruszhang.online/api/不存在
# 期望：{"status":"error","code":"NOT_FOUND","message":"..."}
```

- [x] HTTPS 健康检查返回 db: "connected"
- [x] PM2 进程正常运行（`pm2 list` 显示 online）
- [x] MongoDB 进程正常（`systemctl status mongod`）
- [x] 服务器开机自启已配置
- [x] 博客和 Agent 项目零中断（未触碰）

## ⚠️ 已知限制（下一个任务解决）
- 没有任何业务路由（任务 3 加鉴权 /auth/login）
- WX_APPID / WX_APPSECRET / OWNER_OPENID / CUSTOMER_OPENID 在 .env 里都是空的（任务 3 填）
- 没有数据库 Schema（任务 6 开始加 Mongoose 模型）

## ▶️ 下一步
任务 3：openid 白名单鉴权 + JWT
- 安装 wx-server-sdk 或 axios 调用 code2session
- 新增 POST /api/auth/login（接收 code，返回 token + role）
- 新增 JWT 中间件保护后续业务路由
- 写白名单管理（首次启动从 .env 读，存进 config 表）