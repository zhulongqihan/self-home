# 验收清单 v0.0.1 - 服务器 HTTPS + 项目结构整理

## ✅ 本次产出
- 阿里云服务器装好 Nginx + acme.sh
- Let's Encrypt 多域名证书涵盖 5 个域名（api / 根 / www / blog / agent）
- 主机 Nginx 443 端口对外提供 HTTPS：
  - `https://api.cyruszhang.online` → 反代到本地 3000（待 Node 后端）
  - `https://cyruszhang.online` / www / blog → 反代到博客 Docker 80
  - `https://agent.cyruszhang.online` → 反代到博客 Docker 80
- 证书自动续期脚本 + Cron 任务（每天 3:30 检查，剩余 30 天自动续）
- 项目结构整理：
  - 删除 `cloud/`，新增 `server/`
  - PRD 升级 v2.1（自建后端）
  - 小程序前端去掉 `wx.cloud` 调用，改为 `wx.request` 调用 https
  - 新增 `miniprogram/config/env.js` 管理 API 基址

## 🟢 验收点
- [x] `https://api.cyruszhang.online/healthz` 返回 "ok"
- [x] `https://cyruszhang.online/` 返回 HTTP 200（博客首页）
- [x] 浏览器地址栏显示 🔒（证书有效）
- [x] 博客 5 个容器全部 healthy
- [x] 自动续期脚本 dry-run 正常（剩余天数判断正确）
- [x] GitHub 仓库无敏感信息
- [x] `cloud/` 已删除
- [x] PRD.md v2.1 内容反映自建后端方案

## ⚠️ 服务器侧已做的关键改动（可以回滚）
- `/etc/nginx/conf.d/api.cyruszhang.online.conf` （新增）
- `/etc/nginx/conf.d/blog.cyruszhang.online.conf` （新增）
- `/etc/nginx/conf.d/myblog.conf` → 改名为 `.bak.20260528`（旧的、未挂载的主机 nginx 配置，跟博客容器无关）
- `/etc/nginx/ssl/api.cyruszhang.online/` （证书目录）
- `/usr/local/bin/renew-api-cert.sh` （续期脚本）
- crontab：`30 3 * * * /usr/local/bin/renew-api-cert.sh`
- `/root/.acme.sh/` （acme.sh 安装目录）

## ▶️ 下一步
任务 2：服务器装 MongoDB + Node 后端骨架（Express + Mongoose）
- 装 MongoDB 7.x（本机 27017，仅 127.0.0.1 监听）
- 装 PM2 全局
- 创建 `server/` 后端代码（Express + Mongoose 连接，基础健康检查路由）
- PM2 启动后端，监听 127.0.0.1:3000
- 验证：`curl https://api.cyruszhang.online/api/health` 返回 `{status: "ok"}`