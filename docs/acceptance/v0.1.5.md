# 验收清单 v0.1.5 - 任务 5：账号密码登录 + 混合鉴权

## ✅ 本次产出

### 后端
- `User` 模型升级：字段 `username` / `password_hash`，sparse + unique 索引
- `POST /api/auth/login-password` 新路由（bcrypt 验证）
- 启动 seed：从 `.env` 读取默认账号密码并自动创建用户
- 安全：密码 bcrypt 加密，JWT_SECRET 随机生成

### 前端
- `utils/auth.js`：新增 `loginByPassword`
- `pages/launch/login`：仅密码框（密码同时当 username）
- `pages/launch/index`：openid 失败 → 自动跳密码登录页（不再去兜底页）
- 首页「切换账号」按钮：跳密码登录页（清除 token）
- 首页「退出登录」按钮：回启动页（强制重新 openid 登录）

### 配置
- `.env` 新增 `OWNER_USERNAME/PASSWORD/NICKNAME` + `CUSTOMER_USERNAME/PASSWORD/NICKNAME`
- PRD v2.2 更新：鉴权机制章节

## 🎯 端到端流程（新）

### 首次使用（女朋友扫码）
1. 微信扫码 → 小程序启动
2. `pages/launch/index` → 自动 wx.login → 后端 openid 不在白名单 → 自动跳 `pages/launch/login`
3. 输入暗号 `zhuzhu` → 登录成功 → 进入顾客首页
4. ✅ 你不需要 SSH 改数据库！

### 店长视角切换
1. 你扫码 → openid 在白名单 → 自动进店长工作台
2. 点击「切换账号」→ 输入 `yangyang` → 进入店长工作台（可体验顾客视角）

### 密码修改
- 任务 16 提供后台「系统配置」页面，可在线修改默认账号密码

## 🟢 验收点（小程序端）

### 步骤 1：微信开发者工具 → 重新编译
启动 → 自动跳密码登录页（因为 openid 不在白名单）

### 步骤 2：测试登录
- 输入 `yangyang` → 进入店长工作台
- 输入 `zhuzhu` → 进入顾客首页
- 输入错密码 → 显示「暗号不对哦」

### 步骤 3：切换账号
- 在顾客首页点击「切换账号」→ 重新输入 `yangyang` → 进入店长工作台
- 在店长工作台点击「切换账号」→ 输入 `zhuzhu` → 进入顾客首页

### 步骤 4：退出登录
- 在任一首页点击「退出登录」→ 自动跳回启动页重新尝试 openid 登录

### 步骤 5：openid 登录（你）
- 你的 openid 已在白名单 → 重新编译 → 自动进店长工作台（无感登录）

## ⚠️ 注意
- 密码 `yangyang` / `zhuzhu` 为弱密码，**仅适合情侣私密使用**，上线后请务必通过后台修改
- 建议任务 16 完成后，把默认密码改成你和女朋友容易记但别人猜不到的内容

## ▶️ 下一步
任务 6：商品分类 + 商品列表页面（顾客端）